import Ember from 'ember';

export default Ember.ArrayController.extend({
    itemController: 'deal',
    selectedId: null,
    isEditing: false,

    actions: {
        create: function() {
        },

        edit: function() {
        },
    },
});
